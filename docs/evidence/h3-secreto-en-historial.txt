EVIDENCIA H-3 - El secreto ficticio sigue recuperable en el historial de git
Rama: week4/security-audit-oscar

El arbol de trabajo actual esta limpio. El escaneo obligatorio de la semana 03
revisa unicamente archivos rastreados en HEAD, no el historial.

== 1. Historial del archivo que contuvo la credencial ==

$ git log --oneline -- src/config/backendConfig.ts
42a5aa8 fix: require the backend secret from the environment
1925944 test: reproduce a committed credential failing the mandatory secret scan

== 2. El contenido antiguo sigue disponible en el commit 1925944 ==

$ git show 1925944 --stat --format='%h %an %ad %s' --date=short
1925944 Oscar Espinoza Landeta 2026-09-18 test: reproduce a committed credential failing the mandatory secret scan

 src/config/backendConfig.ts | 11 +++++++++++
 1 file changed, 11 insertions(+)

$ git show 1925944:src/config/backendConfig.ts | grep -c 'API_SECRET'
2
  (numero de lineas de ese archivo antiguo que declaran o leen la variable)

Linea con el valor, enmascarada para no reintroducir el patron en el arbol:
const EXPO_PUBLIC_***_S3CR3T = '<valor ficticio redactado>';

== 3. El arbol actual ya no lo contiene ==

$ git grep -c 'campus-demo' HEAD -- src/ || echo 'sin coincidencias en HEAD'
sin coincidencias en HEAD

Conclusion: quitar un secreto del arbol de trabajo no lo quita del historial.
Cualquiera que clone el repositorio puede recuperarlo con git show.
